import { ValidateGradeDirective } from './validate-grade.directive';

describe('ValidateGradeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateGradeDirective();
    expect(directive).toBeTruthy();
  });
});
